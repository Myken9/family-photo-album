version: '3.8'

services:
  app:
    build: .
    container_name: "app-${PR_ID}"
    ports:
      - "${PR_PORT}:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
    volumes:
      - ../logs:/app/logs
      - ../data:/app/data
